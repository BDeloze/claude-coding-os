---
description: Start a bug fix through the Coding OS (reproduce first, then route by size and surface).
---

Fix a bug using the Coding OS. Use the **orchestrator** subagent first to confirm the project,
classify trivial vs non-trivial and sensitive vs non-sensitive, and route into `flow-bug-fix`.

Reproduce the bug before changing anything; use the `systematic-debugging` skill for non-obvious
causes and write a failing test before the fix.

Bug report: $ARGUMENTS
